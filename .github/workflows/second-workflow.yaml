name: reusable-workflow
on: 
  workflow_call:
    inputs:
      name:
        description: name of the workflow calling this
        default: default
        required: false
        type: string
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploying
        run: echo "deploying workflow by another workflow - ${{ inputs.name }}"